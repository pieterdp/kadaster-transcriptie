version: '3.1'
services:
  mysql:
    image: mariadb:latest
    ports:
      - 8083:3306
    volumes:
      - ./mysql:/var/lib/mysql
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_USER: kadaster
      MARIADB_PASSWORD: kadaster
      MARIADB_DATABASE: kadaster